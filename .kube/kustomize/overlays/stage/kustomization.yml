apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ontada-mcp-stage
nameSuffix: -stage
commonLabels:
  app: admin-mfe-stage

resources:
- ../../base/common

replicas:
- name: admin-mfe
  count: 1

patches:
- target:
    kind: Service
  patch: |-
    - op: remove
      path: "/spec/ports/0/targetPort" 